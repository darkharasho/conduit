[package]
name = "conduit-core"
version = "0.2.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
indexmap = { version = "2", features = ["serde"] }
regex = "1"
thiserror = "1"
toml = "0.8"
